# Example environment variables for Catalog Service

# PostgreSQL connection string (Supabase or local Postgres)
DATABASE_URL=postgresql://postgres:[YOUR_PASSWORD]@localhost:54321/catalog?sslmode=require

# 覆盖 gRPC 监听端口（默认为 config.yaml 中的 0.0.0.0:9000），不建议生产使用
PORT=9000

# 运行环境标签（development / staging / production），影响日志与指标
APP_ENV=development

# 服务名称与版本（写入可观测性维度）
SERVICE_NAME=catalog
SERVICE_VERSION=dev

# 可选：自定义配置文件路径（默认使用 configs/config.yaml）
# CONF_PATH=/absolute/path/to/custom-config.yaml

# GCS / PubSub 相关敏感信息放在 .env 中，具体资源仍在 config.yaml 配置：
# - 如需使用本地 Service Account，可指定凭据文件路径
# GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/upload-signer.json
# - 或提供 Base64 编码的 Service Account JSON（启动脚本解码到临时文件）
# GCS_SIGNER_KEY_B64=BASE64_ENCODED_SERVICE_ACCOUNT_JSON
