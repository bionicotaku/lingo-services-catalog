# Kratos Template 环境变量模板
# 复制为 configs/.env 并按需调整

# 配置文件目录或文件，等同于启动参数 -conf
CONF_PATH=configs
# 服务名称，用于日志/观测标签
SERVICE_NAME=template
# 服务版本号，用于日志/观测标签
SERVICE_VERSION=dev
# 运行环境（development/staging/production）
APP_ENV=development
# PostgreSQL/Supabase 连接串（请替换 PROJECT_REF、PASSWORD、REGION）
DATABASE_URL=postgresql://postgres.[PROJECT_REF]:[YOUR_PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?sslmode=require&search_path=kratos_template

# 其他可选变量 -------------------------------------------------
# 服务部署区域自定义标签
# REGION=us-west-1
# OpenTelemetry 导出方式（stdout/otlp）
# OTEL_EXPORTER=otlp
# OTLP 采集端点（仅 OTEL_EXPORTER=otlp 生效）
# OTEL_ENDPOINT=https://otel-collector.example.com:4317
# 实例 ID，用于日志/观测字段
# INSTANCE_ID=dev-instance
